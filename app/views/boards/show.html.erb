<h1><%= @board.title %></h1>

<p><%= @board.body %></p>

<ul>
  <li><%= link_to "Edit board", edit_board_path(@board) %></li>
  <li><%= link_to "Destroy board", board_path(@board), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

